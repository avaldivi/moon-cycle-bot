name: Trigger Daily Moon Post (X)

on:
  schedule:
    - cron: "0 13 * * *" # 13:00 UTC = 9:00 AM ET (during EDT)
  workflow_dispatch:

jobs:
  ping-daily-x-endpoint:
    runs-on: ubuntu-latest
    steps:
      - name: Call the /daily-x endpoint
        env:
          FLY_APP_URL: ${{ secrets.FLY_APP_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          curl -f -sS -X POST "$FLY_APP_URL/daily-x" \
            -H "Authorization: Bearer $CRON_SECRET"
